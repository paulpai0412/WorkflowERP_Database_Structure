# HTML Output

Final report HTML 必須可離線開啟並保留 evidence。

## 要求

- 不依賴遠端 DB。
- 不包含 credentials。
- 圖表與表格的資料來自 report payload。
- 每個主要指標需可追溯到 SQL execution result。
- Delivery packet 需列出 HTML 路徑、payload 路徑、SQL 路徑與 validator evidence。
